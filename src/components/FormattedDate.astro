---
interface Props {
	date: Date;
	locale?: "en" | "zh-TW";
}

const { date, locale = "zh-TW" } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale === "en" ? "en-US" : "zh-TW", {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
