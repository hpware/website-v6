---
import DynamicPage from "../../../components/site/DynamicPage.astro";

export const prerender = false;

const { slug } = Astro.params;

if (!slug) {
    return Astro.rewrite("/zh-TW/404");
}
---

<DynamicPage locale="zh-TW" slug={slug} />
